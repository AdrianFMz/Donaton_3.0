namespace Donaton.Mobile.Utils
{
    public static class Constants
    {
        // Reemplaza esto con la URL exacta de tu API en Render (sin el /api al final)
        public static string BaseUrl = "https://donaton-api.onrender.com";

        // Esta es la que usará el ApiService
        public static string BaseApiUrl = $"{BaseUrl}/api";
    }
}